import 'package:flutter/material.dart';

class AccountsHome extends StatelessWidget {
  const AccountsHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Accounts Home'),
    );
  }
}
